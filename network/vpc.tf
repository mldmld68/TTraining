# ------------------VPC ------------------------------------------------------------------------------------------------------------
resource "aws_vpc" "vpc-mld" {
  cidr_block = "${var.my_cidr_block}.0.0/16"

  tags {
    Name = "vpc de mld"
  }
}
